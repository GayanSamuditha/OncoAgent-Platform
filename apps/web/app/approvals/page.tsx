"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
const apiBase = process.env.NEXT_PUBLIC_API_BASE_URL ?? "http://localhost:8000";
export default function ApprovalsPage() { const [items, setItems] = useState<Record<string, unknown>[]>([]); const [error, setError] = useState(""); useEffect(() => { fetch(`${apiBase}/api/v1/approvals`, { credentials: "include" }).then((r) => r.json()).then((b) => setItems(b.items ?? [])).catch(() => setError("Approvals unavailable.")); }, []); return <main className="min-h-screen px-5 py-8 sm:px-8 lg:px-12"><div className="mx-auto max-w-6xl"><Link className="text-sm font-semibold text-teal" href="/">← Overview</Link><h1 className="mt-8 text-3xl font-bold text-ink">Approval Queue</h1><p className="mt-3 text-sm text-slate-600">Development actor simulation is active. Reviewers must be different from researchers.</p>{error && <p role="alert" className="mt-5 rounded bg-red-50 p-4 text-red-800">{error}</p>}<div className="mt-6 grid gap-4">{items.length === 0 ? <p className="text-slate-500">No approval requests available.</p> : items.map((item) => <article key={String(item.id)} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="font-semibold text-ink">Approval {String(item.id)}</h2><p className="mt-2 text-sm">Run: {String(item.run_id)} · Status: {String(item.status)}</p><p className="mt-2 text-sm">Requested by: {String(item.requested_by_actor_id)}</p><Link className="mt-3 inline-block font-semibold text-teal" href={`/approvals/${String(item.id)}`}>Review evidence →</Link></article>)}</div></div></main>; }
