"use client";
import Link from "next/link";
const stops = [{ title: "Client problem", text: "Move from a research question to inspectable synthetic evidence." }, { title: "Bounded dataset", text: "Use the 100-patient Synthea dataset with explicit criteria." }, { title: "Governed execution", text: "Plan, retrieve, verify, and preserve provenance through the existing workflow." }, { title: "Human review", text: "A separate reviewer approves or rejects the evidence packet." }, { title: "Durable operations", text: "Temporal/CrewAI, resilience, telemetry, release gates, and security remain linked to real records." }];
export default function GuidedDemoPage() { return <main className="px-5 py-10 sm:px-8 lg:px-12"><div className="mx-auto max-w-5xl"><Link href="/demo" className="text-sm font-semibold text-teal">← Demo Control Center</Link><p className="eyebrow mt-8">Presenter mode</p><h1 className="mt-2 text-4xl font-bold text-ink">The OncoAgent client story</h1><p className="mt-4 max-w-3xl text-lg leading-8 text-slate-600">Walk through the populated records in order. Each stop links back to the actual application surface rather than simulating progress.</p><div className="mt-8 space-y-4">{stops.map((stop, index) => <article key={stop.title} className="card flex gap-5"><div className="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-teal-50 font-bold text-teal-900">{index + 1}</div><div><h2 className="text-lg font-bold">{stop.title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{stop.text}</p></div></article>)}</div><div className="mt-8 flex flex-wrap gap-3"><Link href="/workflow" className="rounded-xl bg-ink px-4 py-3 text-sm font-semibold text-white">Start at Research Workspace</Link><Link href="/observability" className="rounded-xl border border-slate-300 px-4 py-3 text-sm font-semibold">Open operations evidence</Link></div></div></main>; }
