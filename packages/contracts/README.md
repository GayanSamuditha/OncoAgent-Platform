# Contracts

This package is reserved for shared API contract artifacts in a later phase. Phase 0 keeps the authoritative response schemas in the FastAPI application and TypeScript types local to the web application.
