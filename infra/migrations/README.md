# Database migrations

The Phase 0 Alembic environment and migration source live under `apps/api/alembic` so they execute with the backend package. This directory is reserved for infrastructure-level migration notes and future deployment packaging; it intentionally contains no duplicate migration scripts.
